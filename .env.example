# Copy to .env.local and fill in. Nothing here is read at build time.
#
# All the LLM_*/API key settings below are FALLBACKS. The same values can be
# changed at runtime from the UI (齿轮图标 → API 配置), and anything saved there
# wins over what is set here. The dialog shows, per field, whether the value in
# effect came from the settings UI, from this file, or from a built-in default.
# Delete a field in the UI to fall back to the value here again.

# Which provider to use: openai | anthropic | mock
# `mock` streams canned text and needs no key — useful for UI work and tests.
LLM_PROVIDER=mock

# Model id for the selected provider.
# LLM_MODEL=gpt-4o-mini
# LLM_MODEL=claude-3-5-sonnet-latest

# Only the key for the provider you selected is needed.
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: OpenAI-compatible gateway (leave unset for api.openai.com).
# OPENAI_BASE_URL=https://...

# Max tokens per reply (Anthropic only; default 2048).
# LLM_MAX_TOKENS=2048

# SQLite file. Created on first run, along with its directory.
DB_PATH=./data/feynman.db

# Optional shared secret. When set, every /api/* route requires
#   Authorization: Bearer <APP_TOKEN>
# and the web UI asks you for it once (stored in the browser only).
# Leave unset for local use. Set it before exposing this app to a network —
# there is no login, so this token is the only thing between the internet and
# your API key, and /api/settings can be used to *change* the configuration.
# APP_TOKEN=
